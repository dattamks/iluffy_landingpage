{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:css"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && python3 manage.py seed_data && python3 manage.py collectstatic --noinput && gunicorn iluffy_landingpage.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
